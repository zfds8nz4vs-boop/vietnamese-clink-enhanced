# Device findings

No device/runtime activation is claimed by this repository.

The Vietnamese Telex resource is generated as vi.cime data. The repository does
not claim that the CIME table alone reproduces every interactive behavior of
the experimental Lua adapter. Real-client tests should record client version,
pack version, selected input method, exact Telex sequence, candidate result,
and cache/restart behavior.